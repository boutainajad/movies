[build]
  command = "npm run build"
  publish = "dist"

# Ensure all static files (including verification files) are served correctly
[[headers]]
  for = "/*.html"
  [headers.values]
    Content-Type = "text/html; charset=UTF-8"
